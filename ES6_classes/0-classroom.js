// Classe ClassRoom - Représente une salle de classe
export default class ClassRoom {
  // Constructeur qui accepte la taille maximale d'étudiants
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
