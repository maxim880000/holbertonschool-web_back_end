// Test pour la classe ClassRoom
import ClassRoom from './0-classroom.js';

// Créer une instance de ClassRoom avec 10 étudiants max
const room = new ClassRoom(10);
console.log(room._maxStudentsSize);
